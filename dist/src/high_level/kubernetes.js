var Kubernetes = /** @class */ (function () {
    function Kubernetes(name) {
        // create network
    }
    Kubernetes.prototype.add_master = function () {
    };
    Kubernetes.prototype.add_worker = function () {
    };
    return Kubernetes;
}());
